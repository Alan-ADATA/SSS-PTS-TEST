The following are example install steps for this benchmark.


# UBUNTU 16.04LTS SETUP

# install dependencies
$sudo apt-get update
$sudo apt-get -y install fio gnuplot util-linux zip hdparm wkhtmltopdf xvfb git nvme-cli php-cli php-mysql apache2


# install fio-2.16
https://packages.ubuntu.com/artful/fio


# install MariaDB
https://mariadb.com/downloads/mariadb-tx


# install phpMyAdmin
https://www.phpmyadmin.net/


# get SSS-PTS-TEST
$cd /opt
$git clone https://github.com/Alan-ADATA/SSS-PTS-TEST.git
$sudo mkdir -m=777 report


# Set up Database
1. Log in phpMyAdmin and import pts.sql
2. Add user account user name:pts password:pts for pts database
2. Add a new user data into pts.user table


# config apache server
1. $sudo nano /etc/apache2/apache2.conf
    add 
    <Directory /opt/SSS-PTS-TEST/web/>
        Options Indexes FollowSymLinks
        AllowOverride None
        Require all granted
    </Directory>
2. $sudo nano /etc/apache2/sites-available/000-default.conf
    change DocumentRoot /opt/SSS-PTS-TEST/web
3. $sudo service apache2 restart
4. $sudo visudo
    add www-data ALL=(ALL) NOPASSWD: ALL


# run
Use browser and type 'localhost' in address bar.
